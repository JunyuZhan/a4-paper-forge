# 2026-08-18

## A4 纸样工坊（静态站，部署到 Vercel）
- 需求：数百种 A4 打印纸版式（书法/拼音/田字格/方格/坐标/康奈尔等），颜色与格子数量可选，一键打印，按规律分类。
- 实现：纯静态站（index.html + css/style.css + js/{data,paper,app}.js）。
  - `paper.js`：参数化 SVG 渲染器，viewBox 0 0 210 297（mm），25 个版式函数，支持 color / cell / thumb。
  - `data.js`：8 大分类 + 26 个家族，按 颜色×格子尺寸 展开为 **406 种** 版式目录（CATALOG）。
  - `app.js`：侧边分类导航、搜索、画廊、颜色/尺寸按钮、预览、一键打印（@media print 仅输出 A4 纸张）、SVG/PNG 下载、随机。
- 部署：已 git 提交并推送到 GitHub 仓库 `JunyuZhan/a4-paper-forge`（公开）。
  - Vercel CLI 未登录且无 token，无法自动化部署；提供 one-click 导入链接由用户在 Vercel 侧完成导入即上线（纯静态，无需构建）。
- 验证：node 语法检查 + 25 渲染器渲染无 NaN/undefined；本地 http.server 各资源均 200。

## 二次核查与修复（用户反馈：没外框 / 分类筛选像没生效）
- 实证核查：406 种全部渲染 0 异常；各分类命中数 hanzi90/pinyin42/grid84/lined56/math42/draft22/special35/journal35；所有家族 type 均有渲染器；无无效 sizeKey/colorKey。
- 结论：**分类筛选逻辑本来就是对的**（state.cat 变化后 renderGallery 真的过滤），用户误判是因为反馈太弱——画廊标题恒为"全部版式"且画廊在下方，点了没看到变化。
- 修复 1（外框）：`paper.js` 新增 `frame` 选项（默认 true），完整预览/打印时绘距边 8mm 细实线外框（颜色随所选线色），缩略图不画保持干净；`app.js` 加 `frameToggle` 复选框可开关；`blank` 加外框后不再光板。
- 修复 2（筛选反馈）：画廊标题动态显示当前分类（如"✍️ 汉字练习"）；点分类后平滑滚动到画廊；`index.html` 加 `galTitle`/`frameToggle` 节点；`style.css` 加 `.switch` 样式。
- 加固打印：重写 @media print，改为隐藏 `.no-print` 并让 `#sheet` 占满 A4（不再用 body* 全隐藏的粗暴写法，避免误伤 SVG 子元素 display）。
- 已 git 提交并推送到 `origin/master`（仓库 JunyuZhan/a4-paper-forge）。

## 三轮核查与修复（外框细节 + 打印空白）
- 用户质疑 3 点：① 外框是否全加/可自定义 → 答：全局「加外框」开关(默认开)，用户决定；② 有外框时框外不应再有内容 → **真 bug**：原网格从 0 满铺到 210，框外溢出线条。`paper.js` 改用 SVG `clipPath` 把内容裁剪到 (8,8,194,281) 框内，框外不再有线条；缩略图仍不加框。③ 一键打印预览看不到内容 → 真问题：旧打印用 `position:fixed` 有空白风险。`style.css` 改为标准可靠方案（隐藏 `.no-print` + `#sheet` 正常流占满 210×297mm + `@page margin:0`）。
- 验证：全量 406 种 frame=true 渲染异常=0；clipPath/裁剪组/外框矩形结构断言全 true；frame=false 与缩略图均无框；语法 OK。已推送 `origin/master`。

## 四轮核查与修复（页边距不合理 + 分类准确性）
- 用户质疑：① 方形类页边距太小；② 纯横格/无外框类应只需上下页边距、不要左右页边距；③ 分类是否准确（数学类真的数学？）。
- 根因：`geom()` 对所有版式一刀切用对称页边距（原 MIN_M=8，方格最小边距仅 7–10mm），横格也被强制左右留白。
- 修复：`paper.js` 引入 `MODES` 映射 + `_mode`，`geom(cell,thumb,mode)` 分两模式——
  - `box`（方格/点阵/田字格/坐标/算术等 2D 网格）：四边对称 `BOX_M=12mm`，实测各格边距 12–23.5mm，仍整数倍、无半格半行；
  - `ruled`（横线/信纸/笔记/康奈尔/错题本等 1D 横线）：仅上下 `RULED_M=12mm`，横线 `ox=0, iw=210` 左右铺满（红线在 18mm），真正"无左右页边距"。
- 分类修正：`data.js` 将「数学田字格」(底层即田字格渲染器，名不副实) 移出数学类 → 改「数字田字格」归入 汉字练习；数学类现仅含 算术本(带斜线)、竖式计算纸(加粗基准线)，均名副其实。
- 另：错题本/阅读笔记归在「手账」偏"学习笔记"、音乐绘画硬塞一类，已向用户说明、由用户决定是否再调整(未改动)。
- 验证：全量 406 种 render(frame=true)+缩略图 异常=0；box 四边≥12mm 且整数倍全 OK；ruled ox=0/iw=210 且横线起点=18/右端=210；外框矩形跟随几何；语法 OK。已提交推送 `42b9e4d`(origin/master)。

## 五轮优化（页边距应随外框状态变化）
- 用户洞察：页边距主要为外框"离纸边留呼吸感"服务；无外框时大留白无意义，只需最小打印机安全边距让网格铺满。
- `paper.js` 引入 `_frame`（render 按是否加外框设置）+ `BOX_M_OFF=5` / `RULED_M_OFF=5`，`geom` 在 box/ruled 中按 `_frame` 选边距：有框用 12mm 舒适边距，无框用 5mm 安全边距（网格贴边铺满）。
- 验证：全量 406 种 有框/无框 渲染均 0 异常；方格无框左边缘 7mm(有框14mm) 且无外框矩形；无框整数倍仍成立(边距5-7mm)；横格无框顶部 5mm(有框12mm)。已提交推送 `5a65536`(origin/master)。

## 六轮新增（书法/作文/法律文书三大类）
- 用户要求补齐缺失品类：书法纸(毛笔/硬笔、竖向排列)、作文类格式、法律模板(如庭审记录格子)。
- `paper.js` 新增：
  - `vcol` 竖向书法渲染器（inner: none/tian/mi/grid；heavy 控制界格竖线加粗，模拟乌丝栏）；包装 `maobimi`(毛笔米字竖排)/`maobijie`(毛笔界格竖排)/`yingbitian`(硬笔田字竖排)/`yingbige`(硬笔方格竖排)。
  - `zuowen`(作文方格)/`zuowenH`(作文横格)：方格/横格区 + 抬头模板(题目 + 班级/姓名/学号/日期 四框)。
  - `legalBase`(variant: court/bilu/doc) + `legalCourt`/`legalBilu`/`legalDoc`：方格 + 抬头字段模板(标题 + 案号/案由/时间地点/审判人员等填空线)。
  - 文本辅助 `T()`(带 `_thumb` 抑制，模板文字不进缩略图) / `esc()` / `tw()`。
- `data.js`：`CATEGORIES` 增 书法纸(shufa)/作文纸(zuowen)/法律文书(legal) 三类；原 hanzi 下「书法纸」改为毛笔米字格竖排(type maobimi) 移入 shufa；新增对应 FAMILIES。
- 验证：目录 406 → **524 种**；全量(含外框/缩略图)渲染异常=0；三类均含条目(shufa54/zuowen28/legal42)；模板文字完整预览有、缩略图无；毛笔界格加粗竖线=10；整数倍几何保持。已提交推送 `3674db5`(origin/master)。

## 七轮重做（法律文书：庭审记录不应用方格 + 抬头字段太单薄）
- 用户质疑：① 庭审记录正文不该是方格（笔录是写出来的，应横线）；② 抬头当事人字段太简陋，应含 时间/地点/主审/原告/被告/第三人及各自代理人，二审的还应含 上诉人/被上诉人。
- 根因：原 `legalBase(variant:court/bilu)` 正文是方格（gridInner 竖线），抬头仅 6 个短字段。
- 修复 `paper.js`：
  - 新增 `legalRecord(stage)`（stage: court1/court2/bilu）：**横线正文** + 完整案件信息表单（两列、自动避重叠）。
    - 一审(court1)：案号/案由、时间/地点、审判长(主审)/审判员、书记员/陪审员、原告+代理人、被告+代理人、第三人+代理人。
    - 二审(court2)：上诉人+代理人、被上诉人+代理人、原审原告+代理人、原审被告+代理人、第三人+代理人（另含案号/时间/地点/审判人员）。
    - 讯问(bilu)：案号/案由、时间/地点、讯问人/记录人、被讯问人/单位及职务。
    - 表单下加分隔线 + 横线正文（行距=cell）+ 页脚"第__页 共__页"。
  - `legalBase` 仅保留给 `legalDoc`（方格起草稿纸），标题改"法律文书稿纸"，抬头 文书名称/案号/当事人/日期。
  - 新增辅助 `legalField(s,g,x,y,label,boundary,...)`：标签+填空横线，boundary 限制右端避免两列重叠。
  - `legalCourt2` 新渲染器 + MODES/RENDERERS 注册。
- `data.js`： courtroom 家族改名"庭审笔录（一审）"、新增"庭审笔录（二审）"(type legalCourt2)、讯问改名"讯问笔录"、legaldoc 改名"法律文书方格稿纸"（明确是起草方格）。
- 验证：目录 524 → **538 种**；全量(含外框/缩略图)渲染异常=0；四渲染器均 OK；庭审(一审/二审) **竖线=0（无方格✓）**、法律文书稿纸竖线=12（方格✓）；两列真实不重叠(左列右端99.5 < 右列左端118.6mm，约19mm中缝)；缩略图无文字。已提交推送 `8726e73`(origin/master)。
